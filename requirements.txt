# 基础依赖
tkinter>=8.6  # GUI库（通常Python自带）
pywin32>=305  # Windows系统API支持
pillow>=10.0.0  # 图像处理

# 国际化支持
babel>=2.12.1  # 国际化工具
python-i18n>=0.3.9  # 多语言支持

# 打包工具
pyinstaller>=6.0.0  # 用于打包可执行文件
cx-Freeze>=6.15.0  # 可选的打包工具

# 开发工具（可选）
black>=23.0.0  # 代码格式化
flake8>=6.0.0  # 代码检查
mypy>=1.0.0  # 类型检查
pytest>=7.0.0  # 单元测试

# 注意：某些依赖可能需要根据你的Python版本调整版本号 